server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html;

<<<<<<< HEAD
    include /etc/nginx/mime.types;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location /static/ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        try_files $uri =404;
    }

    # Nginx status endpoint for Prometheus
    location /nginx_status {
        stub_status on;
        allow 127.0.0.1;
        deny all;
    }
}
=======
    # Handle React client-side routing
# Nginx status endpoint for Prometheus
    location /nginx_status {
        stub_status on;
        allow all;
    }
>>>>>>> 023271a (Fix: allow all IPs for nginx_status endpoint)
